// Operators in Dart
void main() {
  /* 
    * Spread operators evaluate an expression 
    * that yields a collection, 
    * unpacks the resulting values, 
    * and inserts them into another collection.
  */

  // Coming soon: 
  // Spread operators 
  // on the Collections page
  var a = [10, 20, 30];
  var b = [40, 30, 50];
  print([...a + b]);
}
