/// Dart Keywords: [false]

/// The `false` keyword has a very specific and fundamental use:
/// It represents the negative Boolean value. It is used in 
/// conditional expressions, assignments, comparisons, and more.

void main() { 
  bool isReady = false;

  if(!isReady) {
    print('It\'s not ready');
  }
}
